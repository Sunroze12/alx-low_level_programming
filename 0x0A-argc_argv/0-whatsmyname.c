#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 * @argc: number of arguments passed to the program
 * @argv: array of arguments
 *
 * Return: always 0
 */
int main(int argc, char **argv)
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
